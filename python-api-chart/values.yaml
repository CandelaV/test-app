# Default values for python-api-chart.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

replicaCount: 1

image:
  repository: cvidal/test-app:latest
  pullPolicy: Always


application:
  name: rest-api