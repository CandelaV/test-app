# Default values for python-api-chart.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

replicaCount: 1

image:
  repository: candelav/test-app:monitoring.v1
  pullPolicy: Always


application:
  name: rest-api